---
phase: 04-caching-and-context-notebooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/customer_service/agent/system_prompts.py
  - src/customer_service/agent/context_manager.py
  - src/customer_service/agent/agent_loop.py
  - src/customer_service/agent/__init__.py
  - src/customer_service/anti_patterns/raw_transcript.py
  - src/customer_service/anti_patterns/batch_api_live.py
  - src/customer_service/anti_patterns/__init__.py
  - tests/test_caching.py
  - tests/test_context_manager.py
autonomous: true
requirements: [OPTIM-01, OPTIM-02, ANTI-04]

must_haves:
  truths:
    - "get_system_prompt_with_caching() returns a list of 2 TextBlockParam dicts, second has cache_control"
    - "POLICY_DOCUMENT exceeds 2048 tokens (len // 4 > 2048) to guarantee caching activates"
    - "run_agent_loop accepts both str and list[dict] for system_prompt without code changes"
    - "ContextSummary.token_estimate stays under TOKEN_BUDGET after 10+ updates"
    - "ContextSummary preserves key facts from early turns after compaction"
    - "RawTranscriptContext.token_estimate grows linearly with each append"
  artifacts:
    - path: "src/customer_service/agent/system_prompts.py"
      provides: "POLICY_DOCUMENT constant, get_system_prompt_with_caching()"
      contains: "cache_control"
    - path: "src/customer_service/agent/context_manager.py"
      provides: "ContextSummary class with update, to_system_context, compaction"
      exports: ["ContextSummary", "TOKEN_BUDGET"]
    - path: "src/customer_service/anti_patterns/raw_transcript.py"
      provides: "RawTranscriptContext class demonstrating O(n) growth"
      exports: ["RawTranscriptContext"]
    - path: "src/customer_service/anti_patterns/batch_api_live.py"
      provides: "BATCH_API_EXPLANATION constant with CCA teaching text"
      exports: ["BATCH_API_EXPLANATION"]
    - path: "tests/test_caching.py"
      provides: "Behavioral tests for OPTIM-01 caching"
    - path: "tests/test_context_manager.py"
      provides: "Behavioral tests for OPTIM-02 context management"
  key_links:
    - from: "src/customer_service/agent/system_prompts.py"
      to: "src/customer_service/agent/agent_loop.py"
      via: "system_prompt parameter accepts list[dict] from get_system_prompt_with_caching()"
      pattern: "system_prompt.*str.*list"
    - from: "src/customer_service/agent/context_manager.py"
      to: "notebooks (Phase 4 Plan 02)"
      via: "ContextSummary imported and used in NB05"
      pattern: "from customer_service.agent.context_manager import ContextSummary"
---

<objective>
Build the production code and behavioral tests for prompt caching (OPTIM-01), context management (OPTIM-02), and anti-patterns (ANTI-04).

Purpose: Provide the caching infrastructure, context summary class, and anti-pattern modules that notebooks 04 and 05 will import. All behavioral claims are test-backed.

Output: 6 production files + 2 test files, all passing `poetry run pytest`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CCA-RULES.md
@.planning/phases/04-caching-and-context-notebooks/04-RESEARCH.md

@src/customer_service/agent/system_prompts.py
@src/customer_service/agent/agent_loop.py
@src/customer_service/agent/__init__.py
@src/customer_service/anti_patterns/__init__.py
@notebooks/helpers.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/customer_service/agent/agent_loop.py:
```python
@dataclass
class UsageSummary:
    input_tokens: int = 0
    output_tokens: int = 0
    cache_read_input_tokens: int = 0
    cache_creation_input_tokens: int = 0

def run_agent_loop(
    client: object,
    services: ServiceContainer,
    user_message: str,
    system_prompt: str,  # WIDEN to: str | list[dict]
    model: str = "claude-sonnet-4-6",
    max_tokens: int = 4096,
    max_iterations: int = 10,
    tools: list[dict] | None = None,
    callbacks: dict | None = None,
) -> AgentResult:
```

From src/customer_service/agent/system_prompts.py:
```python
def get_system_prompt() -> str:
    # Returns 31-line plain string prompt
```

From src/customer_service/agent/__init__.py:
```python
__all__ = [
    "AgentResult", "CallbackResult", "UsageSummary",
    "build_callbacks", "get_system_prompt", "run_agent_loop",
]
```

From src/customer_service/anti_patterns/__init__.py:
```python
# Currently exports: run_confidence_agent, run_prompt_compliance_agent, run_swiss_army_agent
# Will add: RawTranscriptContext, BATCH_API_EXPLANATION
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POLICY_DOCUMENT, caching function, agent_loop type widening, anti-patterns, and caching tests</name>
  <files>
    src/customer_service/agent/system_prompts.py
    src/customer_service/agent/agent_loop.py
    src/customer_service/anti_patterns/raw_transcript.py
    src/customer_service/anti_patterns/batch_api_live.py
    src/customer_service/anti_patterns/__init__.py
    tests/test_caching.py
  </files>
  <read_first>
    src/customer_service/agent/system_prompts.py
    src/customer_service/agent/agent_loop.py
    src/customer_service/anti_patterns/__init__.py
    tests/test_agent_loop.py
    .planning/phases/04-caching-and-context-notebooks/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: POLICY_DOCUMENT token count (len(POLICY_DOCUMENT) // 4) exceeds 2048 — CRITICAL threshold
    - Test: get_system_prompt_with_caching() returns list of exactly 2 dicts
    - Test: Block 0 has type "text" and text matches get_system_prompt()
    - Test: Block 1 has type "text", text == POLICY_DOCUMENT, cache_control == {"type": "ephemeral"}
    - Test: get_system_prompt_with_caching() return type is list (not str)
    - Test: run_agent_loop system_prompt parameter accepts list[dict] (mock client test — pass list form, verify client.messages.create received it as system= kwarg)
    - Test: RawTranscriptContext.token_estimate() grows after each append (3 appends, each estimate > previous)
    - Test: RawTranscriptContext.to_context_string() starts with "CONVERSATION HISTORY:"
    - Test: BATCH_API_EXPLANATION is a non-empty string containing "Batch API" and "24"
  </behavior>
  <action>
    **TDD RED phase — write tests/test_caching.py first:**

    Create `tests/test_caching.py` with these test classes:
    - `TestPolicyDocument`: assert `len(POLICY_DOCUMENT) // 4 > 2048` (the 2048-token minimum for claude-sonnet-4-6 caching). Target 2200+ tokens = ~8800+ characters of prose.
    - `TestSystemPromptCaching`: test `get_system_prompt_with_caching()` returns correct list-of-blocks structure. Block 0 = instructions (no cache_control), Block 1 = POLICY_DOCUMENT with `cache_control: {"type": "ephemeral"}`.
    - `TestAgentLoopSystemType`: mock `client.messages.create` and verify it receives the list form when passed `system_prompt=get_system_prompt_with_caching()`. Use the `_make_response` pattern from `test_agent_loop.py`.
    - `TestRawTranscriptAntiPattern`: test O(n) growth of `RawTranscriptContext` — 3 appends, each `token_estimate()` strictly greater than previous.
    - `TestBatchApiExplanation`: test `BATCH_API_EXPLANATION` contains "Batch API" and "24" (24-hour latency).

    **TDD GREEN phase — implement production code:**

    1. **system_prompts.py** — Add `POLICY_DOCUMENT` constant: a comprehensive, detailed refund/return policy document covering tier definitions (Standard/Premium/VIP), per-tier refund limits and timelines, damage categories (manufacturer defect, shipping damage, customer-caused, wear-and-tear), compliance requirements (PCI-DSS, GDPR data handling, audit trail), edge cases (partial refunds, gift returns, international orders, subscription cancellations), and escalation criteria. This must be real, substantial policy text — NOT filler. Target **at least 9000 characters** (~2250 tokens) to safely exceed the 2048-token minimum. Add `get_system_prompt_with_caching() -> list[dict]` that returns the two-block structure per the research pattern.

    2. **agent_loop.py** — Change type hint only: `system_prompt: str` becomes `system_prompt: str | list[dict]`. NO logic change needed — the SDK's `client.messages.create(system=...)` natively accepts both. Update the docstring to note caching support.

    3. **anti_patterns/raw_transcript.py** — Create `RawTranscriptContext` class with `__init__`, `append(role, content)`, `to_context_string()`, and `token_estimate()` methods. Include docstring explaining this is the anti-pattern: O(n) growth, lost-in-middle effect.

    4. **anti_patterns/batch_api_live.py** — Create module with `BATCH_API_EXPLANATION` string constant: a multi-paragraph CCA teaching explanation of why Batch API is wrong for live support (24h latency, not ZDR-eligible, correct answer is Real-Time API + Prompt Caching).

    5. **anti_patterns/__init__.py** — Add imports for `RawTranscriptContext` and `BATCH_API_EXPLANATION`, update `__all__`.

    Run tests after each file. All `test_caching.py` tests must pass.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_caching.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cache_control" src/customer_service/agent/system_prompts.py returns >= 1
    - python -c "from customer_service.agent.system_prompts import POLICY_DOCUMENT; assert len(POLICY_DOCUMENT) // 4 > 2048"
    - grep "str | list" src/customer_service/agent/agent_loop.py returns match
    - grep "RawTranscriptContext" src/customer_service/anti_patterns/raw_transcript.py returns match
    - grep "BATCH_API_EXPLANATION" src/customer_service/anti_patterns/batch_api_live.py returns match
    - poetry run pytest tests/test_caching.py -x passes
  </acceptance_criteria>
  <done>
    POLICY_DOCUMENT > 2048 tokens (verified by test). get_system_prompt_with_caching() returns correct 2-block list with cache_control. agent_loop accepts list[dict] system_prompt. RawTranscriptContext demonstrates O(n) growth. BATCH_API_EXPLANATION provides CCA teaching content. All tests in test_caching.py pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContextSummary class and behavioral tests</name>
  <files>
    src/customer_service/agent/context_manager.py
    src/customer_service/agent/__init__.py
    tests/test_context_manager.py
  </files>
  <read_first>
    .planning/phases/04-caching-and-context-notebooks/04-RESEARCH.md
    src/customer_service/agent/__init__.py
  </read_first>
  <behavior>
    - Test: ContextSummary.update() increments turn_count by 1 per call
    - Test: ContextSummary.update() appends tool_name to tools_called list
    - Test: ContextSummary.update() appends result_summary to decisions_made list
    - Test: ContextSummary.token_estimate == len(summary.to_system_context()) // 4 after every operation (estimate accuracy)
    - Test: After 10 updates with long result_summaries, token_estimate stays <= TOKEN_BUDGET (budget enforcement)
    - Test: After compaction, early tool names still in tools_called (tools_called is append-only, never compacted)
    - Test: to_system_context() contains "SESSION CONTEXT:" header
    - Test: to_system_context() includes customer_id and issue_type when set
    - Test: After setting customer_id and issue_type directly, token_estimate updates on next update() call
    - Test: ContextSummary starts with turn_count=0, empty tools_called, empty decisions_made
  </behavior>
  <action>
    **TDD RED phase — write tests/test_context_manager.py first:**

    Create `tests/test_context_manager.py` with `TestContextSummary` class:
    - `test_initial_state`: fresh ContextSummary has turn_count=0, empty tools_called, empty decisions_made
    - `test_update_increments`: call update("lookup_customer", "found C001"), verify turn_count=1, tools_called=["lookup_customer"], decisions_made contains "found C001"
    - `test_estimate_accuracy`: after each update, assert `summary.token_estimate == len(summary.to_system_context()) // 4`
    - `test_budget_enforcement`: perform 10+ updates with 50-char result_summaries. Assert `summary.token_estimate <= TOKEN_BUDGET` after all updates. This is the key behavioral test — compaction must fire.
    - `test_key_facts_preserved`: set customer_id="C001", issue_type="refund". Do 10 updates. Assert "C001" and "refund" still in `to_system_context()` output (structured fields survive compaction).
    - `test_to_system_context_format`: verify output starts with "SESSION CONTEXT:" and contains customer_id, issue_type
    - `test_tools_called_not_compacted`: after 10 updates, tools_called still has all entries (compaction targets decisions_made, not tools_called — but tools_called shows last N via slicing in output)

    **TDD GREEN phase — implement context_manager.py:**

    Create `src/customer_service/agent/context_manager.py`:
    - `TOKEN_BUDGET = 300` (characters // 4 heuristic — approximately 75 tokens, fits in system block)
    - `ContextSummary` dataclass with fields: customer_id, issue_type, tools_called, decisions_made, pending_actions, turn_count, token_estimate
    - `update(tool_name, result_summary)` — appends, increments turn_count, calls _update_token_estimate, compacts if over budget
    - `to_system_context()` — renders structured text with SESSION CONTEXT header. Shows last 5 tools_called and last 3 decisions_made in output string.
    - `_update_token_estimate()` — `self.token_estimate = len(self.to_system_context()) // 4`
    - `_compact()` — truncates decisions_made to last 2 entries, then recalculates estimate. If still over budget, further truncate pending_actions.

    Update `src/customer_service/agent/__init__.py`:
    - Add import of `ContextSummary` and `TOKEN_BUDGET` from `context_manager`
    - Add both to `__all__`

    Run tests. All `test_context_manager.py` tests must pass. Then run full suite: `poetry run pytest`.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_context_manager.py tests/test_caching.py -x -q && poetry run ruff check src/customer_service/agent/context_manager.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "ContextSummary" src/customer_service/agent/context_manager.py returns match
    - grep "TOKEN_BUDGET" src/customer_service/agent/context_manager.py returns match
    - grep "ContextSummary" src/customer_service/agent/__init__.py returns match
    - poetry run pytest tests/test_context_manager.py -x passes
    - poetry run pytest -x -q passes (full suite green)
    - poetry run ruff check src/ passes
  </acceptance_criteria>
  <done>
    ContextSummary class with update/to_system_context/compaction working. Token estimate stays under TOKEN_BUDGET after 10+ updates (behavioral proof). Key facts (customer_id, issue_type) preserved through compaction. All tests pass. Exported from agent __init__.py.
  </done>
</task>

</tasks>

<verification>
```bash
# All new tests pass
poetry run pytest tests/test_caching.py tests/test_context_manager.py -x -q

# Full suite still green
poetry run pytest -x -q

# Lint clean
poetry run ruff check src/
poetry run ruff format --check src/

# Critical behavioral check: POLICY_DOCUMENT > 2048 tokens
poetry run python -c "from customer_service.agent.system_prompts import POLICY_DOCUMENT; t = len(POLICY_DOCUMENT) // 4; print(f'POLICY_DOCUMENT tokens: {t}'); assert t > 2048, f'BELOW THRESHOLD: {t}'"
```
</verification>

<success_criteria>
- POLICY_DOCUMENT exceeds 2048 tokens (behavioral test + manual check)
- get_system_prompt_with_caching() returns 2-block list with cache_control on block 1
- run_agent_loop accepts str | list[dict] for system_prompt
- ContextSummary stays under TOKEN_BUDGET after 10+ updates
- ContextSummary preserves customer_id and issue_type through compaction
- RawTranscriptContext grows O(n) per append
- BATCH_API_EXPLANATION contains CCA teaching content
- All tests pass, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-caching-and-context-notebooks/04-01-SUMMARY.md`
</output>
