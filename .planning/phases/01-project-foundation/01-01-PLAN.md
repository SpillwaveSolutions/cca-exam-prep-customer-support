---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/customer_service/__init__.py
  - src/customer_service/__main__.py
  - src/customer_service/agent/__init__.py
  - src/customer_service/anti_patterns/__init__.py
  - src/customer_service/data/__init__.py
  - src/customer_service/models/__init__.py
  - src/customer_service/services/__init__.py
  - src/customer_service/tools/__init__.py
  - .pre-commit-config.yaml
  - pyproject.toml
  - tests/__init__.py
  - tests/test_imports.py
  - tests/test_config.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03

must_haves:
  truths:
    - "Running `poetry install` then `python -c 'import customer_service'` succeeds"
    - "All 6 sub-packages are importable: agent, anti_patterns, data, models, services, tools"
    - "`python -m customer_service` prints skeleton message and exits 0"
    - "`.pre-commit-config.yaml` exists with nbstripout and ruff hooks configured"
    - "pre-commit and nbstripout are in dev dependencies"
  artifacts:
    - path: "src/customer_service/__init__.py"
      provides: "Package root with __version__"
      contains: "__version__"
    - path: "src/customer_service/__main__.py"
      provides: "CLI entry point skeleton"
      contains: "def main"
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit hook configuration"
      contains: "nbstripout"
    - path: "tests/test_imports.py"
      provides: "Import smoke tests"
      contains: "import customer_service"
    - path: "tests/test_config.py"
      provides: "Config file existence tests"
      contains: "pre-commit-config"
  key_links:
    - from: "pyproject.toml"
      to: "src/customer_service/__init__.py"
      via: "packages = [{include = 'customer_service', from = 'src'}]"
      pattern: "packages.*customer_service.*src"
    - from: ".pre-commit-config.yaml"
      to: "pyproject.toml"
      via: "ruff reads config from pyproject.toml"
      pattern: "ruff-pre-commit"
---

<objective>
Create the importable Python package skeleton and pre-commit hook configuration.

Purpose: Students need `poetry install` to work and all sub-packages to be importable before any code is added. Pre-commit hooks enforce notebook hygiene and code style from the start.

Output: 8 `__init__.py` files (1 root + 6 sub-packages + 1 tests), `__main__.py` entry point, `.pre-commit-config.yaml`, updated `pyproject.toml` with pre-commit/nbstripout dev deps, and Wave 0 tests for imports and config.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create package __init__.py files, __main__.py, and Wave 0 import tests</name>
  <files>
    src/customer_service/__init__.py,
    src/customer_service/__main__.py,
    src/customer_service/agent/__init__.py,
    src/customer_service/anti_patterns/__init__.py,
    src/customer_service/data/__init__.py,
    src/customer_service/models/__init__.py,
    src/customer_service/services/__init__.py,
    src/customer_service/tools/__init__.py,
    tests/__init__.py,
    tests/test_imports.py
  </files>
  <read_first>
    pyproject.toml,
    .claude/CLAUDE.md,
    CLAUDE.md
  </read_first>
  <behavior>
    - test_import_root: `import customer_service` succeeds and has `__version__` attribute equal to "0.1.0"
    - test_import_agent: `import customer_service.agent` succeeds
    - test_import_anti_patterns: `import customer_service.anti_patterns` succeeds
    - test_import_data: `import customer_service.data` succeeds
    - test_import_models: `import customer_service.models` succeeds
    - test_import_services: `import customer_service.services` succeeds
    - test_import_tools: `import customer_service.tools` succeeds
    - test_main_entry: `from customer_service.__main__ import main` succeeds and `main()` returns 0
  </behavior>
  <action>
    1. Create `tests/__init__.py` as empty file.

    2. Create `tests/test_imports.py` with these tests (RED phase -- tests will fail because __init__.py files don't exist yet):
       - `test_import_root()`: `import customer_service; assert customer_service.__version__ == "0.1.0"`
       - `test_import_agent()`: `import customer_service.agent`
       - `test_import_anti_patterns()`: `import customer_service.anti_patterns`
       - `test_import_data()`: `import customer_service.data`
       - `test_import_models()`: `import customer_service.models`
       - `test_import_services()`: `import customer_service.services`
       - `test_import_tools()`: `import customer_service.tools`
       - `test_main_entry()`: `from customer_service.__main__ import main; result = main(); assert result == 0`

    3. Run tests, confirm they fail (RED).

    4. Create `src/customer_service/__init__.py` (GREEN phase):
       ```python
       """CCA Customer Support Resolution Agent package."""

       __version__ = "0.1.0"
       ```
       Phase 1 keeps this minimal -- NO re-exports of ServiceContainer or run_agent_loop (those modules don't exist yet per Pitfall 5 in RESEARCH.md). Re-exports will be added in Phase 2.

    5. Create 6 sub-package `__init__.py` files, each as:
       ```python
       """CCA Customer Support — {subpackage_name} sub-package."""
       ```
       Files:
       - `src/customer_service/agent/__init__.py` — docstring: "agent sub-package"
       - `src/customer_service/anti_patterns/__init__.py` — docstring: "anti_patterns sub-package"
       - `src/customer_service/data/__init__.py` — docstring: "data sub-package"
       - `src/customer_service/models/__init__.py` — docstring: "models sub-package"
       - `src/customer_service/services/__init__.py` — docstring: "services sub-package"
       - `src/customer_service/tools/__init__.py` — docstring: "tools sub-package"

    6. Create `src/customer_service/__main__.py`:
       ```python
       """Entry point: python -m customer_service

       Phase 1: Skeleton only. Full implementation in Phase 2.
       """
       import sys


       def main() -> int:
           """Run the CCA Customer Support Resolution Agent."""
           print("CCA Customer Support Resolution Agent")
           print("Phase 1: Package skeleton ready.")
           print("Run 'poetry run jupyter lab' to open notebooks.")
           print("See notebooks/00_setup.ipynb to verify your environment.")
           return 0


       if __name__ == "__main__":
           sys.exit(main())
       ```
       Note: return type is `int` (not `None` as in research snippet). The function returns 0 for success, which `sys.exit()` uses.

    7. Run tests again, confirm all pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_imports.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_imports.py contains `def test_import_root` and `assert customer_service.__version__ == "0.1.0"`
    - tests/test_imports.py contains `def test_import_agent`, `def test_import_models`, `def test_import_services`, `def test_import_tools`, `def test_import_data`, `def test_import_anti_patterns`
    - tests/test_imports.py contains `def test_main_entry` and `assert result == 0`
    - src/customer_service/__init__.py contains `__version__ = "0.1.0"`
    - src/customer_service/__main__.py contains `def main() -> int:`
    - src/customer_service/agent/__init__.py exists and is a valid Python file
    - src/customer_service/models/__init__.py exists and is a valid Python file
    - src/customer_service/services/__init__.py exists and is a valid Python file
    - src/customer_service/tools/__init__.py exists and is a valid Python file
    - src/customer_service/data/__init__.py exists and is a valid Python file
    - src/customer_service/anti_patterns/__init__.py exists and is a valid Python file
    - `poetry run pytest tests/test_imports.py -x -q` exits 0 with 8 passed
  </acceptance_criteria>
  <done>All 8 import tests pass. Package is importable. `python -m customer_service` prints skeleton message and returns 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure pre-commit hooks and add dev dependencies</name>
  <files>
    .pre-commit-config.yaml,
    pyproject.toml,
    tests/test_config.py
  </files>
  <read_first>
    pyproject.toml,
    .planning/phases/01-project-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_precommit_config_exists: `.pre-commit-config.yaml` exists
    - test_precommit_has_ruff: `.pre-commit-config.yaml` contains "ruff-pre-commit" and "ruff-check" and "ruff-format"
    - test_precommit_has_nbstripout: `.pre-commit-config.yaml` contains "nbstripout"
    - test_precommit_ruff_jupyter: `.pre-commit-config.yaml` contains "jupyter" in types_or for ruff hooks
    - test_pyproject_has_precommit: `pyproject.toml` dev dependencies include pre-commit
    - test_pyproject_has_nbstripout: `pyproject.toml` dev dependencies include nbstripout
  </behavior>
  <action>
    1. Create `tests/test_config.py` with tests that:
       - Read `.pre-commit-config.yaml` and verify it exists
       - Parse YAML and verify it contains repo entries for `ruff-pre-commit` and `nbstripout`
       - Verify ruff hooks have `types_or: [python, pyi, jupyter]`
       - Verify ruff-check has `args: [--fix]`
       - Read `pyproject.toml` and verify `pre-commit` and `nbstripout` appear in dev dependencies
       Use `pathlib.Path` to locate files relative to project root (use `Path(__file__).resolve().parent.parent` for project root).

    2. Run tests, confirm they fail (RED) -- .pre-commit-config.yaml doesn't exist and pyproject.toml doesn't have the deps yet.

    3. Create `.pre-commit-config.yaml` with exact content:
       ```yaml
       repos:
         - repo: https://github.com/astral-sh/ruff-pre-commit
           rev: v0.15.7
           hooks:
             - id: ruff-check
               types_or: [python, pyi, jupyter]
               args: [--fix]
             - id: ruff-format
               types_or: [python, pyi, jupyter]

         - repo: https://github.com/kynan/nbstripout
           rev: 0.9.1
           hooks:
             - id: nbstripout
       ```
       Hook ordering: ruff-check (with --fix) before ruff-format. Both use `types_or` (not `types`) to include jupyter files. Do NOT run `nbstripout --install` (that is git filter mode, not pre-commit hook mode -- see Pitfall 2 in RESEARCH.md).

    4. Update `pyproject.toml` to add `pre-commit` and `nbstripout` to the dev dependencies group:
       ```toml
       [tool.poetry.group.dev.dependencies]
       pytest = ">=8.0"
       ruff = ">=0.4"
       mypy = ">=1.10"
       pre-commit = ">=3.0"
       nbstripout = ">=0.7"
       ```

    5. Run `poetry lock --no-update` to update the lock file with new deps, then `poetry install`.

    6. Run `pre-commit install` to activate hooks in the local git repo.

    7. Run tests again, confirm all pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - .pre-commit-config.yaml contains `repo: https://github.com/astral-sh/ruff-pre-commit`
    - .pre-commit-config.yaml contains `rev: v0.15.7`
    - .pre-commit-config.yaml contains `id: ruff-check`
    - .pre-commit-config.yaml contains `id: ruff-format`
    - .pre-commit-config.yaml contains `types_or: [python, pyi, jupyter]`
    - .pre-commit-config.yaml contains `args: [--fix]`
    - .pre-commit-config.yaml contains `repo: https://github.com/kynan/nbstripout`
    - .pre-commit-config.yaml contains `rev: 0.9.1`
    - .pre-commit-config.yaml contains `id: nbstripout`
    - pyproject.toml contains `pre-commit = ">=3.0"`
    - pyproject.toml contains `nbstripout = ">=0.7"`
    - `poetry run pytest tests/test_config.py -x -q` exits 0
  </acceptance_criteria>
  <done>Pre-commit config exists with ruff (check + format for python + jupyter) and nbstripout hooks. Dev deps updated. Tests pass.</done>
</task>

</tasks>

<verification>
All Plan 01 checks:
1. `poetry run pytest tests/test_imports.py tests/test_config.py -x -q` -- all tests pass
2. `python -c "import customer_service; print(customer_service.__version__)"` -- prints "0.1.0"
3. `python -m customer_service` -- prints skeleton message, exits 0
4. `cat .pre-commit-config.yaml` -- contains ruff-check, ruff-format, nbstripout
</verification>

<success_criteria>
- All 6 sub-packages importable via `from customer_service.{name} import ...`
- `python -m customer_service` prints skeleton and exits 0
- `.pre-commit-config.yaml` has ruff + nbstripout hooks
- pre-commit and nbstripout in pyproject.toml dev deps
- All tests in test_imports.py and test_config.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
