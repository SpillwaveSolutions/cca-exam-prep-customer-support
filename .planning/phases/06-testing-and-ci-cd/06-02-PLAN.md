---
phase: 06-testing-and-ci-cd
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
  - notebooks/08_meta_teaching.ipynb
  - notebooks/07_integration.ipynb
autonomous: true
requirements: [CICD-03]

must_haves:
  truths:
    - "README contains a meta-teaching section explaining how the project itself follows CCA patterns"
    - "Notebook 08 walks students through the project's own CCA artifacts interactively"
    - "At least 2 existing notebooks have inline meta-teaching callouts connecting to CCA patterns"
  artifacts:
    - path: "README.md"
      provides: "CCA Meta-Patterns in This Project section"
      contains: "Meta-Patterns"
    - path: "notebooks/08_meta_teaching.ipynb"
      provides: "Interactive meta-teaching notebook"
      contains: "meta"
    - path: "notebooks/07_integration.ipynb"
      provides: "Updated with inline meta-teaching callout"
      contains: "CCA Meta"
  key_links:
    - from: "README.md"
      to: ".claude/CLAUDE.md"
      via: "references actual file path"
      pattern: "\\.claude/CLAUDE\\.md"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "references actual file path"
      pattern: "\\.github/workflows/ci\\.yml"
    - from: "README.md"
      to: ".claude/skills/review-cca-compliance.md"
      via: "references actual file path"
      pattern: "\\.claude/skills/"
---

<objective>
Create meta-teaching content in three places: README section, standalone notebook 08, and inline callouts in existing notebooks.

Purpose: Students learn not just the CCA patterns in the code but also how the project's own infrastructure (CLAUDE.md, CI/CD, custom skills, pre-commit hooks) exemplifies those same patterns. This is the "meta-teaching layer" -- the project teaches by example at every level.

Output: Updated `README.md`, new `notebooks/08_meta_teaching.ipynb`, updated existing notebooks with inline callouts.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CCA-RULES.md
@.planning/phases/06-testing-and-ci-cd/06-CONTEXT.md
@.claude/CLAUDE.md
@CLAUDE.md
@README.md

<interfaces>
<!-- The executor needs to know the existing README structure to append to it -->
From README.md:
- Has: Quick Start, Overview sections
- Ends with: "See `CLAUDE.md` for full architecture documentation."
- Meta-teaching section should be appended before the CLAUDE.md reference or replace that line

From notebooks/ directory:
- 00_setup.ipynb through 07_integration.ipynb exist
- 08_meta_teaching.ipynb is NEW
- Notebook convention: markdown cells with HTML boxes for CCA Exam Tips
- Pattern: setup cells, anti-pattern section (red), correct-pattern section (green), comparison

From Plan 06-01 (dependency):
- .github/workflows/ci.yml will exist (created by Plan 01)
- .claude/skills/review-cca-compliance.md will exist (created by Plan 01)
- NB08 code cells that read these files will work because Plan 01 runs first (Wave 1 -> Wave 2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meta-teaching section to README and create notebook 08</name>
  <files>README.md, notebooks/08_meta_teaching.ipynb</files>
  <read_first>
    - README.md (current content to append to)
    - .planning/CCA-RULES.md (CI/CD rules, CLAUDE.md hierarchy, custom skills)
    - .planning/phases/06-testing-and-ci-cd/06-CONTEXT.md (locked decisions on meta-teaching)
    - .claude/CLAUDE.md (project standards to reference)
    - notebooks/07_integration.ipynb (existing notebook format/conventions -- read first 30 lines for structure)
    - notebooks/00_setup.ipynb (notebook format reference -- read first 30 lines)
  </read_first>
  <action>
**README.md -- Add "CCA Meta-Patterns in This Project" section:**

Add a new section between "Overview" and the final CLAUDE.md reference. Content:

```
## CCA Meta-Patterns in This Project

This project doesn't just *teach* CCA patterns — it *uses* them. Here's how:

### CLAUDE.md Hierarchy (Level 2: Project)
- `.claude/CLAUDE.md` — Team standards enforced project-wide (CCA exam: project-level is VCS-tracked, shared across team)
- `CLAUDE.md` (root) — Architecture docs and build commands
- See: CCA-RULES.md "CLAUDE.md Hierarchy" section

### CI/CD Pipeline Flags
- `.github/workflows/ci.yml` — Uses `claude -p --bare --output-format json --allowedTools Read,Grep,Glob`
- `-p`: mandatory for non-interactive CI (without it, pipeline hangs)
- `--bare`: reproducibility (no CLAUDE.md loaded in CI agent)
- `--allowedTools`: principle of least privilege for CI agents
- See: Notebook 08 for interactive walkthrough

### Custom Skill
- `.claude/skills/review-cca-compliance.md` — Invoke to review any code for CCA compliance
- Demonstrates: custom skills as reusable, project-specific instructions

### Programmatic Enforcement (Pre-commit Hooks)
- `.pre-commit-config.yaml` — nbstripout + ruff enforced on every commit
- Same principle as callbacks: code enforces rules, not human memory
```

Keep the existing Quick Start and Overview sections intact. Reference actual file paths that exist in the repo.

**Notebook 08 (notebooks/08_meta_teaching.ipynb):**

Create a Jupyter notebook following the project's established notebook conventions. This is NOT an anti-pattern notebook -- it's a walkthrough of the project's own CCA artifacts.

Structure:
1. **Title cell**: "Notebook 08: CCA Meta-Teaching -- How This Project Practices What It Preaches"
2. **Introduction**: Explain that students have seen 6 CCA patterns in the code; now they'll see how the project's infrastructure uses the same patterns
3. **Section 1 -- CLAUDE.md Hierarchy**: Code cell that reads and displays `.claude/CLAUDE.md` using `pathlib.Path`. Markdown cell explaining the 4-level hierarchy (managed, project, user, local) and why project-level is VCS-tracked.
4. **Section 2 -- CI/CD Pipeline**: Code cell that reads and displays `.github/workflows/ci.yml`. Markdown cell walking through each flag with CCA exam significance. Include a CCA Exam Tip box: "On the exam, if a pipeline is 'hanging', the answer is almost always: missing -p flag."
5. **Section 3 -- Custom Skill**: Code cell that reads and displays `.claude/skills/review-cca-compliance.md`. Markdown cell explaining custom skills as reusable instructions.
6. **Section 4 -- Pre-commit Hooks**: Code cell that reads `.pre-commit-config.yaml`. Markdown cell connecting hooks to "programmatic enforcement beats prompt-based guidance."
7. **Section 5 -- Pattern Connections**: Summary table connecting each project infrastructure element to its CCA pattern equivalent (hooks = callbacks, CI flags = structured output, CLAUDE.md = context management).
8. **CCA Exam Tip boxes** using the same HTML styling as other notebooks (red/green bordered boxes).

Use `pathlib.Path` for all file reads. All paths relative to project root (use `Path("..") / ".claude" / "CLAUDE.md"` from notebooks dir or determine project root programmatically).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && grep -q "Meta-Patterns" README.md && echo "README section exists" && grep -q ".claude/CLAUDE.md" README.md && echo "README refs CLAUDE.md" && grep -q ".github/workflows" README.md && echo "README refs CI workflow" && grep -q ".claude/skills/" README.md && echo "README refs skills" && test -f notebooks/08_meta_teaching.ipynb && echo "NB08 exists" && python -c "import json; nb=json.load(open('notebooks/08_meta_teaching.ipynb')); print(f'{len(nb[\"cells\"])} cells in NB08')"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "CCA Meta-Patterns in This Project" section
    - README references actual paths: .claude/CLAUDE.md, .github/workflows/ci.yml, .claude/skills/review-cca-compliance.md, .pre-commit-config.yaml
    - notebooks/08_meta_teaching.ipynb exists with 10+ cells covering CLAUDE.md hierarchy, CI flags, custom skill, pre-commit hooks
    - NB08 uses CCA Exam Tip HTML boxes matching existing notebook style
    - NB08 code cells use pathlib.Path for file reads
  </acceptance_criteria>
  <done>README has meta-teaching section referencing real file paths; NB08 is interactive walkthrough of project's CCA infrastructure</done>
</task>

<task type="auto">
  <name>Task 2: Add inline meta-teaching callouts to existing notebooks</name>
  <files>notebooks/07_integration.ipynb</files>
  <read_first>
    - notebooks/07_integration.ipynb (full content -- need to find insertion points)
    - notebooks/01_escalation.ipynb (read first 50 lines -- check existing CCA Exam Tip format)
  </read_first>
  <action>
Add inline meta-teaching callouts to the integration notebook (07) where it naturally demonstrates CCA patterns that the project's own infrastructure also uses.

**Notebook 07 (integration) -- add 2-3 callout cells:**

Find the sections where patterns are demonstrated and add a markdown cell AFTER the demonstration with a meta-teaching note. Use a distinct HTML box style (blue border) to differentiate from the existing CCA Exam Tip boxes (which use red/green):

```html
<div style="border-left: 4px solid #2196F3; padding: 10px; margin: 10px 0; background-color: #E3F2FD;">
<strong>CCA Meta-Pattern:</strong> [connection to project infrastructure]
</div>
```

Specific callouts to add:

1. **After the callbacks/enforcement section**: "This project's own pre-commit hooks (.pre-commit-config.yaml) follow the same principle -- programmatic enforcement (ruff, nbstripout) rather than relying on developers to remember code style rules."

2. **After the tool design section or near the end**: "The project's CI pipeline (.github/workflows/ci.yml) uses --allowedTools to sandbox the Claude reviewer to Read, Grep, and Glob only -- the same 'focused tools' principle you just saw with 5 tools vs 15."

3. **In the summary/conclusion section**: "For a hands-on walkthrough of how this project uses CCA patterns in its own infrastructure, see Notebook 08: Meta-Teaching."

Keep all existing content intact. Only ADD new markdown cells at appropriate points. Do not modify any existing cells.

Per user decision, inline callouts go in existing notebooks where they naturally connect. NB07 (integration) is the primary target since it covers all 6 patterns. If natural insertion points exist in other notebooks, the executor may add 1-2 callouts there as well, but NB07 is the minimum requirement.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && python -c "import json; nb=json.load(open('notebooks/07_integration.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='markdown' and 'Meta-Pattern' in ''.join(c['source'])]; print(f'{len(cells)} meta-teaching callouts in NB07')" && python -c "import json; nb=json.load(open('notebooks/07_integration.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='markdown' and 'Meta-Pattern' in ''.join(c['source'])]; assert len(cells) >= 2, f'Need 2+ callouts, found {len(cells)}'"</automated>
  </verify>
  <acceptance_criteria>
    - notebooks/07_integration.ipynb has 2+ new markdown cells with "CCA Meta-Pattern" callouts
    - Callouts use blue-bordered HTML box style (distinct from red/green CCA Exam Tips)
    - Callouts reference actual project files (.pre-commit-config.yaml, .github/workflows/ci.yml)
    - Existing notebook content is unchanged (only new cells added)
    - At least one callout mentions Notebook 08 for further exploration
  </acceptance_criteria>
  <done>Integration notebook has inline meta-teaching callouts connecting CCA patterns to project infrastructure; references NB08</done>
</task>

</tasks>

<verification>
- README.md "Meta-Patterns" section references 4 actual file paths that exist
- NB08 has 10+ cells with interactive file-reading code cells
- NB07 has 2+ "CCA Meta-Pattern" callout cells with blue border styling
- No existing notebook content modified (only additions)
- `poetry run ruff check notebooks/` passes (if notebooks are linted)
</verification>

<success_criteria>
Meta-teaching content exists in all three locations (README, NB08, inline callouts) with cross-references between them. Students can follow the trail from README to NB08 for deep exploration, or encounter callouts organically while working through existing notebooks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-ci-cd/06-02-SUMMARY.md`
</output>
