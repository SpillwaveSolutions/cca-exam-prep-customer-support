---
phase: 03-callbacks-enforcement-and-first-notebooks
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - notebooks/01_escalation.ipynb
  - notebooks/02_compliance.ipynb
  - notebooks/03_tool_design.ipynb
  - tests/test_notebooks.py
autonomous: true
requirements: [NB-02, NB-03, NB-04]

must_haves:
  truths:
    - "Notebook 01 runs end-to-end showing confidence anti-pattern approving $600 refund, then callback pattern escalating it"
    - "Notebook 02 runs end-to-end showing prompt compliance logging raw PII, then callback pattern redacting it"
    - "Notebook 03 runs end-to-end showing 15-tool agent misrouting, then 5-tool agent succeeding"
    - "Each notebook follows Setup > Anti-Pattern (red) > Correct (green) > Compare template"
    - "Each notebook displays token usage via print_usage and comparison via compare_results"
  artifacts:
    - path: "notebooks/01_escalation.ipynb"
      provides: "Escalation pattern notebook (C003 $600)"
      contains: "CCA Exam Tip"
    - path: "notebooks/02_compliance.ipynb"
      provides: "Compliance pattern notebook (C001 + PII)"
      contains: "CCA Exam Tip"
    - path: "notebooks/03_tool_design.ipynb"
      provides: "Tool design pattern notebook (C003 $600, 15 vs 5 tools)"
      contains: "CCA Exam Tip"
    - path: "tests/test_notebooks.py"
      provides: "Smoke tests verifying notebook existence and section structure"
  key_links:
    - from: "notebooks/01_escalation.ipynb"
      to: "src/customer_service/anti_patterns/confidence_escalation.py"
      via: "imports run_confidence_agent"
      pattern: "from customer_service.anti_patterns.confidence_escalation import"
    - from: "notebooks/01_escalation.ipynb"
      to: "src/customer_service/agent/agent_loop.py"
      via: "imports run_agent_loop for correct pattern"
      pattern: "from customer_service.agent.agent_loop import"
    - from: "notebooks/02_compliance.ipynb"
      to: "src/customer_service/anti_patterns/prompt_compliance.py"
      via: "imports run_prompt_compliance_agent"
      pattern: "from customer_service.anti_patterns.prompt_compliance import"
    - from: "notebooks/03_tool_design.ipynb"
      to: "src/customer_service/anti_patterns/swiss_army_agent.py"
      via: "imports run_swiss_army_agent"
      pattern: "from customer_service.anti_patterns.swiss_army_agent import"
---

<objective>
Create notebooks 01 (Escalation), 02 (Compliance), and 03 (Tool Design) following the established template. Each demonstrates an anti-pattern failure then the correct CCA pattern, with token usage comparison and CCA Exam Tip boxes.

Purpose: These are the primary teaching artifacts. Students run them and observe the contrast between wrong and right approaches.
Output: 3 runnable Jupyter notebooks plus structural smoke tests.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/CCA-RULES.md
@.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md
@.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-01-SUMMARY.md
@.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-02-SUMMARY.md

<interfaces>
<!-- Code the notebooks import -->

From src/customer_service/anti_patterns/confidence_escalation.py (Plan 02):
```python
CONFIDENCE_SYSTEM_PROMPT: str  # prompt with confidence self-rating
def run_confidence_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult
```

From src/customer_service/anti_patterns/prompt_compliance.py (Plan 02):
```python
PROMPT_COMPLIANCE_SYSTEM_PROMPT: str  # prompt with "never log cards"
def run_prompt_compliance_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult
```

From src/customer_service/anti_patterns/swiss_army_agent.py (Plan 02):
```python
SWISS_ARMY_TOOLS: list[dict]  # 15 tools
DISTRACTOR_TOOLS: list[dict]  # 10 distractors
def run_swiss_army_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult
```

From src/customer_service/agent/agent_loop.py (Plan 01 updated):
```python
def run_agent_loop(client, services, user_message, system_prompt, model, max_tokens, max_iterations, callbacks) -> AgentResult
```

From src/customer_service/agent/callbacks.py (Plan 01):
```python
def build_default_callbacks() -> list[Callable]
```

From src/customer_service/agent/system_prompts.py:
```python
def get_system_prompt() -> str
```

From notebooks/helpers.py:
```python
def print_usage(response: object, model: str = "claude-sonnet-4-6") -> None
def compare_results(anti_result: dict, correct_result: dict) -> None
```

From src/customer_service/data/scenarios.py:
```python
SCENARIOS["amount_threshold"]  # C003, $600 refund
SCENARIOS["happy_path"]        # C001, $50 refund
```

From src/customer_service/services/container.py:
```python
class ServiceContainer: customer_db, policy_engine, financial_system, escalation_queue, audit_log
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke tests for notebook structure</name>
  <files>tests/test_notebooks.py</files>
  <read_first>
    - notebooks/00_setup.ipynb
    - tests/test_helpers.py
    - .planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md
  </read_first>
  <action>
    Create tests/test_notebooks.py with structural smoke tests. These verify notebook files exist and contain the expected sections WITHOUT running any cells or calling the API.

    Use `nbformat.read()` to parse each notebook and inspect cell sources.

    Tests:
    1. `test_notebook_01_exists()` — notebooks/01_escalation.ipynb exists and is valid nbformat v4
    2. `test_notebook_02_exists()` — notebooks/02_compliance.ipynb exists and is valid nbformat v4
    3. `test_notebook_03_exists()` — notebooks/03_tool_design.ipynb exists and is valid nbformat v4
    4. `test_notebook_01_has_sections()` — contains markdown cells with: "Setup", "Anti-Pattern", "Correct", "Compare", "CCA Exam Tip"
    5. `test_notebook_02_has_sections()` — same section check
    6. `test_notebook_03_has_sections()` — same section check
    7. `test_notebook_01_imports_anti_pattern()` — a code cell contains "from customer_service.anti_patterns.confidence_escalation import"
    8. `test_notebook_02_imports_anti_pattern()` — a code cell contains "from customer_service.anti_patterns.prompt_compliance import"
    9. `test_notebook_03_imports_anti_pattern()` — a code cell contains "from customer_service.anti_patterns.swiss_army_agent import"
    10. `test_notebook_01_uses_print_usage()` — a code cell contains "print_usage"
    11. `test_notebook_02_uses_print_usage()` — a code cell contains "print_usage"
    12. `test_notebook_03_uses_print_usage()` — a code cell contains "print_usage"
    13. `test_notebook_01_uses_compare_results()` — a code cell contains "compare_results"
    14. `test_notebook_02_uses_compare_results()` — a code cell contains "compare_results"
    15. `test_notebook_03_uses_compare_results()` — a code cell contains "compare_results"

    Use a helper function to load a notebook and search cells:
    ```python
    def _load_notebook(name: str):
        path = Path(__file__).parent.parent / "notebooks" / name
        assert path.exists(), f"Notebook {name} not found at {path}"
        return nbformat.read(str(path), as_version=4)

    def _has_markdown_containing(nb, text: str) -> bool:
        return any(text.lower() in cell.source.lower() for cell in nb.cells if cell.cell_type == "markdown")

    def _has_code_containing(nb, text: str) -> bool:
        return any(text in cell.source for cell in nb.cells if cell.cell_type == "code")
    ```

    Run tests — they MUST fail (notebooks don't exist yet).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_notebooks.py -x -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notebooks.py has 15 test functions
    - Tests use nbformat to parse notebooks structurally
    - Tests will fail until Task 2 creates the notebooks
  </acceptance_criteria>
  <done>Smoke test file exists and covers existence, sections, imports, and helper usage for all 3 notebooks.</done>
</task>

<task type="auto">
  <name>Task 2: Create notebooks 01, 02, 03 using nbformat</name>
  <files>notebooks/01_escalation.ipynb, notebooks/02_compliance.ipynb, notebooks/03_tool_design.ipynb</files>
  <read_first>
    - notebooks/00_setup.ipynb
    - notebooks/helpers.py
    - src/customer_service/anti_patterns/confidence_escalation.py
    - src/customer_service/anti_patterns/prompt_compliance.py
    - src/customer_service/anti_patterns/swiss_army_agent.py
    - src/customer_service/agent/agent_loop.py
    - src/customer_service/agent/callbacks.py
    - src/customer_service/agent/system_prompts.py
    - src/customer_service/data/scenarios.py
    - src/customer_service/data/customers.py
    - tests/test_notebooks.py (just created in Task 1)
    - .planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md
    - .planning/CCA-RULES.md
  </read_first>
  <action>
    Create a Python script that generates all 3 notebooks using nbformat. Run the script to produce the .ipynb files. The script itself is throwaway (not committed) — only the notebooks matter.

    **Common patterns across all 3 notebooks:**

    Every notebook starts with these cells:
    1. Title markdown: `# Notebook NN: [Pattern Name]\n\n[1-2 sentence description]`
    2. Setup code cell:
    ```python
    import sys
    from pathlib import Path

    # Add project root and notebooks dir to path
    project_root = str(Path(".").resolve().parent)
    if project_root not in sys.path:
        sys.path.insert(0, project_root)
    notebooks_dir = str(Path(".").resolve())
    if notebooks_dir not in sys.path:
        sys.path.insert(0, notebooks_dir)

    import anthropic
    from customer_service.services.customer_db import CustomerDatabase
    from customer_service.services.policy_engine import PolicyEngine
    from customer_service.services.financial_system import FinancialSystem
    from customer_service.services.escalation_queue import EscalationQueue
    from customer_service.services.audit_log import AuditLog
    from customer_service.services.container import ServiceContainer
    from helpers import print_usage, compare_results

    client = anthropic.Anthropic()
    ```
    3. Services setup code cell (fresh for each section to avoid state bleed):
    ```python
    def make_services():
        return ServiceContainer(
            customer_db=CustomerDatabase(),
            policy_engine=PolicyEngine(),
            financial_system=FinancialSystem(),
            escalation_queue=EscalationQueue(),
            audit_log=AuditLog(),
        )
    ```

    **HTML box styles** (match 00_setup.ipynb pattern):
    - Red box (anti-pattern): `<div style="background-color: #ffebee; border-left: 4px solid #f44336; padding: 12px; margin: 10px 0;"><strong>...ANTI-PATTERN...</strong>...</div>`
    - Green box (correct): `<div style="background-color: #e8f5e9; border-left: 4px solid #4caf50; padding: 12px; margin: 10px 0;"><strong>...CORRECT PATTERN...</strong>...</div>`
    - CCA Exam Tip: `<div style="background-color: #fff3e0; border-left: 4px solid #ff9800; padding: 12px; margin: 10px 0;"><strong>CCA Exam Tip:</strong> ...</div>`

    ---

    **Notebook 01: Escalation Pattern (01_escalation.ipynb)**

    Scenario: C003, $600 refund (amount_threshold scenario)

    Sections:
    - **Title**: "# Notebook 01: Escalation Pattern\n\nDeterministic business rules vs. LLM confidence-based routing for escalation decisions."
    - **Setup** (common cells above + scenario-specific):
    ```python
    SCENARIO = "I need a $600 refund for my damaged order #ORD-003."
    CUSTOMER_ID = "C003"
    print(f"Scenario: Customer {CUSTOMER_ID} requests $600 refund")
    print(f"Threshold rule: amount > $500 requires human review")
    ```

    - **Anti-Pattern Section** (red box markdown):
    ```
    ## Anti-Pattern: LLM Confidence-Based Escalation

    <div style="background-color: #ffebee; border-left: 4px solid #f44336; padding: 12px; margin: 10px 0;">
    <strong>ANTI-PATTERN: Self-Reported Confidence Routing</strong><br>
    The system prompt asks Claude to rate its own confidence (0-100) and only escalate if below 70.
    This is probabilistic and unreliable -- Claude typically reports high confidence even on cases
    that should be escalated.
    </div>
    ```

    Anti-pattern code cell:
    ```python
    from customer_service.anti_patterns.confidence_escalation import (
        run_confidence_agent,
        CONFIDENCE_SYSTEM_PROMPT,
    )

    print("System prompt excerpt (confidence routing):")
    print(CONFIDENCE_SYSTEM_PROMPT[-200:])
    print("\n" + "="*60)

    services_anti = make_services()
    anti_result = run_confidence_agent(client, services_anti, SCENARIO)

    print(f"\nStop reason: {anti_result.stop_reason}")
    print(f"Tool calls: {[tc['name'] for tc in anti_result.tool_calls]}")
    print(f"\nAgent response:\n{anti_result.final_text[:500]}")

    # Check if refund was processed (it shouldn't be for $600!)
    refunds = services_anti.financial_system.get_processed()
    escalations = services_anti.escalation_queue.get_escalations()
    print(f"\nRefunds processed: {len(refunds)}")
    print(f"Escalations created: {len(escalations)}")
    if refunds:
        print(f"PROBLEM: $600 refund was approved without human review!")
    ```

    Print usage cell: `print_usage(anti_result, "claude-sonnet-4-6")` (note: print_usage expects a response with .usage, but AgentResult has .usage as UsageSummary — need adapter. Create a simple wrapper: `class _UsageAdapter: def __init__(self, ar): self.usage = ar.usage`. Or pass anti_result directly if print_usage uses getattr.)

    Actually, looking at print_usage, it calls `response.usage.input_tokens` etc. AgentResult has `usage: UsageSummary` with the same fields. So `print_usage(anti_result)` should work directly since it accesses `response.usage.input_tokens`.

    - **Correct Pattern Section** (green box markdown):
    ```
    ## Correct Pattern: Deterministic Callback Enforcement

    <div style="background-color: #e8f5e9; border-left: 4px solid #4caf50; padding: 12px; margin: 10px 0;">
    <strong>CORRECT PATTERN: PostToolUse Callbacks</strong><br>
    Business rules enforced in code (callbacks.py). The callback checks: amount > $500,
    VIP tier, account closure flag, legal keywords. Any match forces escalation
    regardless of what the LLM "thinks."
    </div>
    ```

    Correct code cell:
    ```python
    from customer_service.agent.agent_loop import run_agent_loop
    from customer_service.agent.system_prompts import get_system_prompt

    services_correct = make_services()
    correct_result = run_agent_loop(
        client, services_correct, SCENARIO, get_system_prompt()
    )

    print(f"Stop reason: {correct_result.stop_reason}")
    print(f"Tool calls: {[tc['name'] for tc in correct_result.tool_calls]}")
    print(f"\nAgent response:\n{correct_result.final_text[:500]}")

    refunds = services_correct.financial_system.get_processed()
    escalations = services_correct.escalation_queue.get_escalations()
    print(f"\nRefunds processed: {len(refunds)}")
    print(f"Escalations created: {len(escalations)}")
    if escalations:
        print(f"CORRECT: Escalated with reason: {escalations[0].escalation_reason}")
    ```

    Print usage cell for correct pattern.

    - **Compare Section**:
    ```python
    compare_results(
        {
            "Refunds processed": len(services_anti.financial_system.get_processed()),
            "Escalations created": len(services_anti.escalation_queue.get_escalations()),
            "Correctly escalated": len(services_anti.escalation_queue.get_escalations()) > 0,
        },
        {
            "Refunds processed": len(services_correct.financial_system.get_processed()),
            "Escalations created": len(services_correct.escalation_queue.get_escalations()),
            "Correctly escalated": len(services_correct.escalation_queue.get_escalations()) > 0,
        },
    )
    ```

    - **CCA Exam Tip** (orange box):
    ```
    <div style="background-color: #fff3e0; border-left: 4px solid #ff9800; padding: 12px; margin: 10px 0;">
    <strong>CCA Exam Tip:</strong> If an exam answer mentions "confidence threshold" or
    "self-reported confidence score" for escalation routing, it is ALWAYS the wrong answer.
    The correct approach is deterministic business rules enforced in code (PostToolUse callbacks),
    not LLM self-assessment.
    </div>
    ```

    ---

    **Notebook 02: Compliance Pattern (02_compliance.ipynb)**

    Scenario: C001 happy path with PII in message: "I'd like a $50 refund for order #ORD-001. The item was defective. My card is 4111-1111-1111-1111 for the refund."

    Same structure: Title > Setup > Anti-Pattern (red) > Correct (green) > Compare > CCA Exam Tip

    Anti-pattern: `run_prompt_compliance_agent(client, services, message)` with callbacks=[] — then check `services.audit_log.get_entries()` for raw card number in details.

    Correct: `run_agent_loop(client, services, message, get_system_prompt())` with default callbacks — then check audit log entries for redacted card "****-****-****-1111".

    Compare: `compare_results({"PII in audit log": True/False for each}, ...)`

    CCA Exam Tip: "If an exam answer says 'include PCI compliance requirements in the system prompt,' it is wrong. Compliance enforcement must be programmatic (validation hooks, regex redaction) -- prompts are probabilistic guidance, not deterministic enforcement."

    ---

    **Notebook 03: Tool Design Pattern (03_tool_design.ipynb)**

    Scenario: C003, $600 refund (same as NB01 — different failure mode)

    Anti-pattern: `run_swiss_army_agent(client, services, SCENARIO)` — check which tools were called, expect `file_billing_dispute` or other misroute.

    Correct: `run_agent_loop(client, services, SCENARIO, get_system_prompt())` — check tools called, expect `lookup_customer`, `check_policy`, then escalation.

    Compare: `compare_results({"Tool count available": 15 vs 5, "Correct tool selected": False vs True, "Total tools called": ...}, ...)`

    Show SWISS_ARMY_TOOLS count and list tool names.

    CCA Exam Tip: "If a scenario has 12+ tools and the agent selects the wrong one, the correct fix is to reduce to 4-5 focused tools per agent. Use the coordinator-subagent pattern for workflows requiring more tools."

    ---

    **Generation approach:**

    Write a Python script `/tmp/gen_notebooks.py` that uses nbformat:
    ```python
    import nbformat
    nb = nbformat.v4.new_notebook()
    nb.cells = [
        nbformat.v4.new_markdown_cell("# Title..."),
        nbformat.v4.new_code_cell("import ..."),
        ...
    ]
    nbformat.write(nb, "notebooks/01_escalation.ipynb")
    ```

    Run it: `poetry run python /tmp/gen_notebooks.py`

    Then run the smoke tests to verify structure.

    **Important: print_usage compatibility.** The `print_usage` helper expects `response.usage.input_tokens`. The `AgentResult` has `usage: UsageSummary` with the same fields. So passing `AgentResult` directly to `print_usage` will work because `print_usage` accesses `response.usage` (which resolves to `AgentResult.usage` = `UsageSummary` instance with `input_tokens`, `output_tokens`, etc.).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_notebooks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - notebooks/01_escalation.ipynb exists, valid nbformat v4, has Setup/Anti-Pattern/Correct/Compare sections
    - notebooks/02_compliance.ipynb exists, valid nbformat v4, has all sections
    - notebooks/03_tool_design.ipynb exists, valid nbformat v4, has all sections
    - Each notebook imports its anti-pattern module and the correct agent_loop
    - Each notebook calls print_usage for both anti-pattern and correct runs
    - Each notebook calls compare_results in the Compare section
    - Each notebook has a CCA Exam Tip box (orange, blockquote style)
    - NB01 uses C003 $600 scenario
    - NB02 uses C001 with card "4111-1111-1111-1111" in message
    - NB03 uses C003 $600 scenario
    - All 15 smoke tests in test_notebooks.py pass
    - `poetry run pytest -x -q` full suite passes
    - `poetry run ruff check tests/test_notebooks.py` passes
  </acceptance_criteria>
  <done>3 notebooks created following the established template. Smoke tests verify structure. Each demonstrates anti-pattern failure then correct CCA pattern with token comparison.</done>
</task>

</tasks>

<verification>
1. `poetry run pytest tests/test_notebooks.py -x -q` — all 15 notebook smoke tests pass
2. `poetry run pytest -x -q` — full test suite passes
3. `poetry run ruff check src/ tests/` — lint clean
4. Visual check: open each .ipynb file and confirm section ordering, HTML boxes, imports
</verification>

<success_criteria>
- 3 notebooks follow Setup > Anti-Pattern > Correct > Compare template
- Each notebook imports anti-pattern and correct-pattern code
- Each uses print_usage and compare_results helpers
- Each has a CCA Exam Tip box with exam-specific guidance
- NB01 (Escalation): C003 $600 confidence vs callbacks
- NB02 (Compliance): C001 + PII prompt vs callbacks
- NB03 (Tool Design): C003 $600 15-tool vs 5-tool
- 15 smoke tests verify structure without API calls
</success_criteria>

<output>
After completion, create `.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-03-SUMMARY.md`
</output>
