---
phase: 03-callbacks-enforcement-and-first-notebooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/customer_service/anti_patterns/__init__.py
  - src/customer_service/anti_patterns/confidence_escalation.py
  - src/customer_service/anti_patterns/prompt_compliance.py
  - src/customer_service/anti_patterns/swiss_army_agent.py
  - tests/test_anti_patterns.py
autonomous: true
requirements: [ANTI-01, ANTI-02, ANTI-03]

must_haves:
  truths:
    - "confidence_escalation.py has a system prompt telling Claude to self-rate confidence and handle cases above 70%"
    - "prompt_compliance.py has a system prompt telling Claude to never log credit card numbers, but no programmatic enforcement"
    - "swiss_army_agent.py defines 15 tool schemas (5 correct + 10 distractors with plausible overlapping descriptions)"
    - "Anti-pattern modules live ONLY in src/customer_service/anti_patterns/"
    - "Anti-pattern modules use the same agent loop and services -- only prompts/tools differ"
  artifacts:
    - path: "src/customer_service/anti_patterns/confidence_escalation.py"
      provides: "run_confidence_agent() with confidence-based system prompt"
      exports: ["run_confidence_agent", "CONFIDENCE_SYSTEM_PROMPT"]
    - path: "src/customer_service/anti_patterns/prompt_compliance.py"
      provides: "run_prompt_compliance_agent() with prompt-only redaction rules"
      exports: ["run_prompt_compliance_agent", "PROMPT_COMPLIANCE_SYSTEM_PROMPT"]
    - path: "src/customer_service/anti_patterns/swiss_army_agent.py"
      provides: "run_swiss_army_agent() with 15 tool schemas"
      exports: ["run_swiss_army_agent", "SWISS_ARMY_TOOLS"]
    - path: "tests/test_anti_patterns.py"
      provides: "Unit tests for anti-pattern module structure"
      min_lines: 40
  key_links:
    - from: "src/customer_service/anti_patterns/confidence_escalation.py"
      to: "src/customer_service/agent/agent_loop.py"
      via: "reuses run_agent_loop with different system prompt"
      pattern: "run_agent_loop"
    - from: "src/customer_service/anti_patterns/swiss_army_agent.py"
      to: "src/customer_service/tools/definitions.py"
      via: "imports TOOLS and extends with 10 distractors"
      pattern: "TOOLS|SWISS_ARMY_TOOLS"
---

<objective>
Create three anti-pattern modules that demonstrate CCA exam traps: confidence-based escalation, prompt-only compliance, and 15-tool Swiss Army agent.

Purpose: These deliberately wrong implementations are imported by notebooks to show observable failures. Students see the anti-pattern fail, then see the correct pattern succeed on the same scenario.
Output: Three anti-pattern modules in anti_patterns/ with tests verifying structure.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CCA-RULES.md
@.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md

<interfaces>
<!-- Existing code the anti-patterns build on -->

From src/customer_service/agent/agent_loop.py:
```python
def run_agent_loop(
    client, services, user_message, system_prompt,
    model="claude-sonnet-4-6", max_tokens=4096, max_iterations=10
) -> AgentResult:
```

From src/customer_service/tools/definitions.py:
```python
TOOLS: list[dict] = [
    LOOKUP_CUSTOMER_TOOL, CHECK_POLICY_TOOL, PROCESS_REFUND_TOOL,
    ESCALATE_TO_HUMAN_TOOL, LOG_INTERACTION_TOOL,
]
```

From src/customer_service/agent/system_prompts.py:
```python
def get_system_prompt() -> str:
```

From src/customer_service/data/scenarios.py:
```python
SCENARIOS: dict[str, dict] = {
    "happy_path": { "customer_id": "C001", ... },
    "amount_threshold": { "customer_id": "C003", "message": "I need a $600 refund..." },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three anti-pattern modules with structural tests</name>
  <files>
    src/customer_service/anti_patterns/confidence_escalation.py,
    src/customer_service/anti_patterns/prompt_compliance.py,
    src/customer_service/anti_patterns/swiss_army_agent.py,
    src/customer_service/anti_patterns/__init__.py,
    tests/test_anti_patterns.py
  </files>
  <read_first>
    src/customer_service/anti_patterns/__init__.py,
    src/customer_service/agent/agent_loop.py,
    src/customer_service/agent/system_prompts.py,
    src/customer_service/tools/definitions.py,
    src/customer_service/data/scenarios.py,
    .planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md
  </read_first>
  <behavior>
    - confidence_escalation.py exports CONFIDENCE_SYSTEM_PROMPT (str) and run_confidence_agent(client, services, user_message) -> AgentResult
    - CONFIDENCE_SYSTEM_PROMPT contains "rate your confidence" and "below 70" and "escalate" instructions
    - run_confidence_agent calls run_agent_loop with CONFIDENCE_SYSTEM_PROMPT and NO callbacks (anti-pattern: no programmatic enforcement)
    - prompt_compliance.py exports PROMPT_COMPLIANCE_SYSTEM_PROMPT (str) and run_prompt_compliance_agent(client, services, user_message) -> AgentResult
    - PROMPT_COMPLIANCE_SYSTEM_PROMPT contains "never log credit card" and "redact" instructions
    - run_prompt_compliance_agent calls run_agent_loop with PROMPT_COMPLIANCE_SYSTEM_PROMPT and NO callbacks (anti-pattern: prompt-only enforcement)
    - swiss_army_agent.py exports SWISS_ARMY_TOOLS (list of 15 dicts), SWISS_ARMY_SYSTEM_PROMPT (str), and run_swiss_army_agent(client, services, user_message) -> AgentResult
    - SWISS_ARMY_TOOLS has exactly 15 tools: 5 correct + 10 distractors
    - Distractor tools have plausible, overlapping descriptions (not obviously wrong)
    - file_billing_dispute description overlaps with process_refund (canonical misroute target for $600 refund)
    - create_support_ticket description overlaps with escalate_to_human (canonical misroute target for closure/legal)
    - run_swiss_army_agent calls run_agent_loop but overrides tools parameter (needs a variant of agent_loop or direct client call)
    - Test: CONFIDENCE_SYSTEM_PROMPT contains "confidence" and "70"
    - Test: PROMPT_COMPLIANCE_SYSTEM_PROMPT contains "credit card" and "redact"
    - Test: len(SWISS_ARMY_TOOLS) == 15
    - Test: all 5 correct tool names present in SWISS_ARMY_TOOLS
    - Test: "file_billing_dispute" in SWISS_ARMY_TOOLS names
    - Test: "create_support_ticket" in SWISS_ARMY_TOOLS names
    - Test: each distractor tool has "name", "description", "input_schema" keys
    - Test: no distractor description is empty or less than 20 chars
  </behavior>
  <action>
    Write tests/test_anti_patterns.py FIRST with all structural tests above. These are deterministic tests that verify module structure, NOT API-dependent behavior.

    Then create the three anti-pattern modules:

    1. **confidence_escalation.py**:
       - CONFIDENCE_SYSTEM_PROMPT: A full system prompt that includes customer support context PLUS the anti-pattern instruction: "Before taking any action on a refund or escalation, rate your confidence from 0-100 that you can handle this case correctly. If your confidence is below 70, use escalate_to_human. If 70 or above, proceed to handle the case yourself including processing refunds."
       - This is the CCA trap: Claude will report high confidence and try to handle a $600 refund itself instead of escalating.
       - run_confidence_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult: calls run_agent_loop with CONFIDENCE_SYSTEM_PROMPT. Does NOT pass callbacks (the whole point: no programmatic enforcement).

    2. **prompt_compliance.py**:
       - PROMPT_COMPLIANCE_SYSTEM_PROMPT: Customer support context PLUS: "IMPORTANT COMPLIANCE RULE: Never log credit card numbers, Social Security numbers, or other PII in interaction logs. Always redact sensitive data before logging. Replace card numbers with ****-****-****-NNNN format."
       - This is the CCA trap: Claude will sometimes log the raw card number anyway because prompts are probabilistic.
       - run_prompt_compliance_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult: calls run_agent_loop with PROMPT_COMPLIANCE_SYSTEM_PROMPT. Does NOT pass callbacks.

    3. **swiss_army_agent.py**:
       - 10 distractor tool schemas (each with name, description, input_schema matching Claude API format):
         1. check_shipping_status - "Check the shipping and delivery status of a customer order. Returns tracking info, carrier, and estimated delivery date."
         2. lookup_order_history - "Look up a customer's complete order history including past purchases, returns, and exchanges."
         3. check_inventory - "Check current inventory levels and availability for a specific product or SKU."
         4. update_billing_info - "Update a customer's billing information including payment method and billing address."
         5. file_billing_dispute - "File a billing dispute or chargeback claim for a customer transaction. Initiates the dispute resolution process for contested charges." (CANONICAL MISROUTE: overlaps with process_refund for $600)
         6. create_support_ticket - "Create a new support ticket for tracking and follow-up on unresolved customer issues. Routes to appropriate support team." (CANONICAL MISROUTE: overlaps with escalate_to_human for closure/legal)
         7. reset_password - "Reset a customer's account password and send a temporary credential via email."
         8. update_account_settings - "Update customer account preferences and notification settings."
         9. search_knowledge_base - "Search the internal knowledge base for policy documents, FAQs, and resolution procedures."
         10. send_feedback_survey - "Send a post-interaction customer satisfaction survey via email."
       - Each distractor has a minimal but valid input_schema (at least customer_id or similar).
       - SWISS_ARMY_TOOLS = TOOLS (from definitions.py) + distractor list = 15 total.
       - SWISS_ARMY_SYSTEM_PROMPT: Same customer support context. No special instructions about tool selection (the model must handle 15 tools on its own).
       - run_swiss_army_agent(client, services, user_message, model="claude-sonnet-4-6") -> AgentResult: This needs to use the 15-tool list instead of TOOLS. Create a variant that calls client.messages.create directly in a loop (similar to run_agent_loop but with SWISS_ARMY_TOOLS). OR add an optional `tools` parameter to run_agent_loop. Choose whichever is cleaner -- adding `tools: list[dict] | None = None` to run_agent_loop (defaulting to TOOLS) is simpler and avoids code duplication. If modifying agent_loop signature, ensure backward compat.

    4. **anti_patterns/__init__.py**: Export all three run functions and their prompts/tools for notebook imports.

    All anti-pattern code is deliberately wrong -- that is its purpose. Do NOT add callbacks, redaction, or enforcement to anti-pattern modules. They must fail in the expected way.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_anti_patterns.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CONFIDENCE_SYSTEM_PROMPT" src/customer_service/anti_patterns/confidence_escalation.py
    - grep -q "run_confidence_agent" src/customer_service/anti_patterns/confidence_escalation.py
    - grep -q "confidence" src/customer_service/anti_patterns/confidence_escalation.py
    - grep -q "PROMPT_COMPLIANCE_SYSTEM_PROMPT" src/customer_service/anti_patterns/prompt_compliance.py
    - grep -q "run_prompt_compliance_agent" src/customer_service/anti_patterns/prompt_compliance.py
    - grep -q "credit card" src/customer_service/anti_patterns/prompt_compliance.py
    - grep -q "SWISS_ARMY_TOOLS" src/customer_service/anti_patterns/swiss_army_agent.py
    - grep -q "file_billing_dispute" src/customer_service/anti_patterns/swiss_army_agent.py
    - grep -q "create_support_ticket" src/customer_service/anti_patterns/swiss_army_agent.py
    - grep -q "run_swiss_army_agent" src/customer_service/anti_patterns/swiss_army_agent.py
    - grep -q "def test_.*15" tests/test_anti_patterns.py
    - grep -q "def test_.*distractor" tests/test_anti_patterns.py
  </acceptance_criteria>
  <done>
    Three anti-pattern modules exist in anti_patterns/ with correct exports. SWISS_ARMY_TOOLS has exactly 15 tools with plausible distractor descriptions. All structural tests pass. No programmatic enforcement in any anti-pattern module. All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
poetry run pytest tests/test_anti_patterns.py -x -q
poetry run pytest tests/ -x -q
poetry run ruff check src/customer_service/anti_patterns/ tests/test_anti_patterns.py
</verification>

<success_criteria>
- Three anti-pattern files exist in src/customer_service/anti_patterns/
- SWISS_ARMY_TOOLS has exactly 15 tools with 10 plausible distractors
- file_billing_dispute and create_support_ticket are the canonical misroute targets
- No callbacks or programmatic enforcement in any anti-pattern module
- All structural tests pass
- Ruff lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-02-SUMMARY.md`
</output>
