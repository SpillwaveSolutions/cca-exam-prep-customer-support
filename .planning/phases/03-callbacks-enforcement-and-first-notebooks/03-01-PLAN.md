---
phase: 03-callbacks-enforcement-and-first-notebooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/customer_service/agent/callbacks.py
  - src/customer_service/tools/handlers.py
  - src/customer_service/tools/process_refund.py
  - src/customer_service/agent/agent_loop.py
  - src/customer_service/agent/__init__.py
  - tests/test_callbacks.py
autonomous: true
requirements: [ENFORCE-01, ENFORCE-02, ENFORCE-03]

must_haves:
  truths:
    - "Per-tool callback registry dispatches callbacks ONLY for their registered tool"
    - "process_refund callback blocks $600 refund and returns blocked JSON with action_required=escalate_to_human"
    - "FinancialSystem is untouched after a blocked refund (veto guarantee)"
    - "log_interaction callback redacts credit card patterns before audit write"
    - "lookup_customer callback detects VIP and account_closure flags"
    - "check_policy callback detects amount > $500 requires_review"
  artifacts:
    - path: "src/customer_service/agent/callbacks.py"
      provides: "CallbackResult dataclass, per-tool callback functions, build_callbacks() factory"
      exports: ["CallbackResult", "escalation_callback", "compliance_callback", "build_callbacks"]
    - path: "src/customer_service/tools/handlers.py"
      provides: "Extended dispatch() with context and callbacks parameters"
      contains: "callbacks.get(tool_name)"
    - path: "src/customer_service/tools/process_refund.py"
      provides: "Two-step vetoable refund: propose then commit"
      contains: "propose_refund"
    - path: "tests/test_callbacks.py"
      provides: "Unit tests for all callback rules and veto guarantee"
      min_lines: 80
  key_links:
    - from: "src/customer_service/tools/handlers.py"
      to: "src/customer_service/agent/callbacks.py"
      via: "callbacks dict parameter in dispatch()"
      pattern: "callbacks\\.get\\(tool_name\\)"
    - from: "src/customer_service/tools/handlers.py"
      to: "src/customer_service/tools/process_refund.py"
      via: "two-step vetoable dispatch for process_refund"
      pattern: "propose_refund|action.*block"
    - from: "src/customer_service/agent/agent_loop.py"
      to: "src/customer_service/tools/handlers.py"
      via: "dispatch() call with context and callbacks"
      pattern: "dispatch\\(.*context.*callbacks"
---

<objective>
Build the PostToolUse callback enforcement layer with per-tool callback registry, two-step vetoable dispatch for process_refund, escalation rules, and compliance redaction.

Purpose: This is CCA Principle #1 in action -- programmatic enforcement beats prompt-based guidance. Callbacks enforce business rules deterministically in code.
Output: callbacks.py with all rules, modified dispatch() and agent_loop, passing tests.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CCA-RULES.md
@.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-CONTEXT.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From src/customer_service/tools/handlers.py:
```python
DISPATCH: dict[str, Callable[[dict, ServiceContainer], str]] = { ... }

def dispatch(tool_name: str, input_dict: dict, services: ServiceContainer) -> str:
    # Current signature -- will be extended with context, callbacks
```

From src/customer_service/tools/process_refund.py:
```python
def handle_process_refund(input_dict: dict, services: ServiceContainer) -> str:
    # Currently calls services.financial_system.process_refund() directly
    # Must be refactored: propose step (no commit) + commit step (after callback approval)
```

From src/customer_service/agent/agent_loop.py:
```python
def run_agent_loop(
    client, services, user_message, system_prompt,
    model="claude-sonnet-4-6", max_tokens=4096, max_iterations=10
) -> AgentResult:
    # Line 112: result_content = dispatch(block.name, block.input, services)
    # Must pass context and callbacks to dispatch
```

From src/customer_service/services/financial_system.py:
```python
class FinancialSystem:
    def process_refund(self, customer_id, order_id, amount, policy_approved=True) -> dict:
    def get_processed(self) -> list[dict]:  # Returns copy of all processed records
```

From src/customer_service/services/policy_engine.py:
```python
class PolicyEngine:
    _REVIEW_THRESHOLD = 500.0  # amounts > $500 require review
    def check_policy(self, tier, requested_amount) -> PolicyResult:
```

From src/customer_service/models/customer.py:
```python
class CustomerProfile(BaseModel):
    customer_id: str
    name: str
    email: str
    tier: CustomerTier
    flags: list[str] = []

class PolicyResult(BaseModel):
    approved: bool
    limit: float
    requires_review: bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CallbackResult dataclass, per-tool callbacks, and test suite</name>
  <files>
    src/customer_service/agent/callbacks.py,
    tests/test_callbacks.py
  </files>
  <read_first>
    src/customer_service/tools/handlers.py,
    src/customer_service/tools/process_refund.py,
    src/customer_service/services/policy_engine.py,
    src/customer_service/services/financial_system.py,
    src/customer_service/data/customers.py,
    src/customer_service/models/customer.py,
    tests/conftest.py
  </read_first>
  <behavior>
    - CallbackResult dataclass has fields: action (Literal["allow", "replace_result", "block"]), replacement (str | None), reason (str | None)
    - build_callbacks() returns dict[str, Callable] mapping tool_name -> callback function
    - escalation_callback registered for "process_refund": blocks if ANY of (context has vip=True, account_closure=True, legal_complaint=True, requires_review=True). Returns action="block", replacement=JSON with {"status": "blocked", "reason": "...", "action_required": "escalate_to_human"}
    - compliance_callback registered for "log_interaction": returns action="replace_result" with credit card numbers redacted (****-****-****-1234 pattern). If no PII found, returns action="allow"
    - lookup_callback registered for "lookup_customer": sets context flags (vip, account_closure) from customer profile. Returns action="allow"
    - policy_callback registered for "check_policy": sets context requires_review flag from result. Returns action="allow"
    - Legal keyword detection: scan context["user_message"] for keywords (lawsuit, attorney, lawyer, legal action, sue, court). Sets context["legal_complaint"]=True
    - Test: C003 $600 refund -> escalation_callback returns action="block"
    - Test: C002 VIP -> escalation_callback returns action="block"
    - Test: C004 account_closure flag -> escalation_callback returns action="block"
    - Test: message with "lawsuit" -> legal_complaint flag set, escalation_callback returns action="block"
    - Test: C001 $50 refund, no flags -> escalation_callback returns action="allow"
    - Test: log details with "4111-1111-1111-1111" -> compliance_callback returns action="replace_result" with "****-****-****-1111"
    - Test: log details without card numbers -> compliance_callback returns action="allow"
  </behavior>
  <action>
    Write tests/test_callbacks.py FIRST with all behavior tests above. Tests import from customer_service.agent.callbacks. Use the existing ServiceContainer from conftest.py fixtures.

    Then create src/customer_service/agent/callbacks.py:

    1. CallbackResult dataclass with action: Literal["allow", "replace_result", "block"], replacement: str | None = None, reason: str | None = None

    2. LEGAL_KEYWORDS = ["lawsuit", "attorney", "lawyer", "legal action", "sue", "court"]

    3. CARD_PATTERN = regex for 16-digit card numbers with optional separators (dashes/spaces). Captures last 4 digits for partial redaction.

    4. Per-tool callback functions (each takes tool_name, input_dict, result_dict, context, services):
       - lookup_customer_callback: reads result_dict for tier and flags, sets context["vip"], context["account_closure"]. Also scans context["user_message"] for legal keywords, sets context["legal_complaint"]. Returns CallbackResult(action="allow")
       - check_policy_callback: reads result_dict for requires_review, sets context["requires_review"]. Returns CallbackResult(action="allow")
       - process_refund_callback: checks context flags (vip, account_closure, legal_complaint, requires_review). If ANY True, returns CallbackResult(action="block", replacement=json.dumps({"status": "blocked", "reason": "<which_flag>", "action_required": "escalate_to_human"}), reason="<human readable>")
       - log_interaction_callback: regex-replaces card numbers in result_dict details with "****-****-****-NNNN". If any replacements made, returns CallbackResult(action="replace_result", replacement=<redacted JSON>). Otherwise action="allow"

    5. build_callbacks() -> dict[str, Callable]: returns {"lookup_customer": lookup_customer_callback, "check_policy": check_policy_callback, "process_refund": process_refund_callback, "log_interaction": log_interaction_callback}

    Type annotations on all public functions. No Any types.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/test_callbacks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class CallbackResult" src/customer_service/agent/callbacks.py
    - grep -q "def build_callbacks" src/customer_service/agent/callbacks.py
    - grep -q "action.*block" src/customer_service/agent/callbacks.py
    - grep -q "action_required.*escalate_to_human" src/customer_service/agent/callbacks.py
    - grep -q "def test_.*block.*refund" tests/test_callbacks.py
    - grep -q "def test_.*redact" tests/test_callbacks.py
    - grep -q "def test_.*vip" tests/test_callbacks.py
    - grep -q "def test_.*legal" tests/test_callbacks.py
  </acceptance_criteria>
  <done>
    All callback functions pass unit tests. CallbackResult dataclass exported. build_callbacks() returns per-tool dict. Escalation blocks on VIP/closure/legal/amount. Compliance redacts card numbers. No blocked refund touches FinancialSystem (tested in Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-step vetoable dispatch, agent_loop integration, and veto guarantee test</name>
  <files>
    src/customer_service/tools/handlers.py,
    src/customer_service/tools/process_refund.py,
    src/customer_service/agent/agent_loop.py,
    src/customer_service/agent/__init__.py,
    tests/test_callbacks.py
  </files>
  <read_first>
    src/customer_service/tools/handlers.py,
    src/customer_service/tools/process_refund.py,
    src/customer_service/agent/agent_loop.py,
    src/customer_service/agent/__init__.py,
    src/customer_service/services/financial_system.py,
    tests/test_callbacks.py,
    tests/test_tools.py,
    tests/test_agent_loop.py
  </read_first>
  <behavior>
    - dispatch() accepts context: dict = None, callbacks: dict[str, Callable] | None = None (backward compatible)
    - dispatch() does: cb = callbacks.get(tool_name) if callbacks else None. If cb exists, call it AFTER handler runs (for most tools). For process_refund: call propose_refund first (no FinancialSystem commit), run callback on proposed result, then either commit or return blocked result.
    - process_refund handler split into propose_refund(input_dict, services) -> dict (returns proposed result WITHOUT writing to FinancialSystem) and commit_refund(proposed, services) -> str (writes to FinancialSystem and returns JSON)
    - For process_refund in dispatch: (1) call propose_refund, (2) run callback on proposed result, (3) if action="block" return blocked replacement JSON, (4) if action="allow" call commit_refund
    - For log_interaction in dispatch: run handler, then run callback. If action="replace_result", return the replacement (redacted) instead of original
    - For lookup_customer/check_policy in dispatch: run handler, then run callback (sets context flags). Return original result.
    - agent_loop passes context={"user_message": user_message} and callbacks to dispatch()
    - agent_loop accepts optional callbacks parameter (default None)
    - VETO GUARANTEE TEST: Create ServiceContainer, dispatch process_refund for C003 $600 with escalation callback -> get_processed() returns empty list
    - ALLOW TEST: dispatch process_refund for C001 $50 with escalation callback -> get_processed() has one record
    - BACKWARD COMPAT TEST: dispatch without callbacks still works (all existing tests pass)
  </behavior>
  <action>
    Write additional tests in tests/test_callbacks.py for the veto guarantee and dispatch integration FIRST:
    - test_veto_guarantee_financial_system_untouched: fresh ServiceContainer, dispatch("process_refund", C003 $600 input, services, context with requires_review=True, callbacks with process_refund_callback). Assert services.financial_system.get_processed() == []. Assert result JSON has status="blocked".
    - test_allow_commits_to_financial_system: fresh ServiceContainer, dispatch("process_refund", C001 $50 input, services, context with no flags, callbacks). Assert len(services.financial_system.get_processed()) == 1.
    - test_dispatch_backward_compatible: dispatch("lookup_customer", ..., services) without context/callbacks still returns valid JSON.
    - test_compliance_redaction_in_dispatch: dispatch("log_interaction", ..., services, context, callbacks) with card number in details. Assert returned JSON has redacted card.

    Then modify the production code:

    1. **process_refund.py**: Add `propose_refund(input_dict, services) -> dict` that does everything handle_process_refund does EXCEPT calling services.financial_system.process_refund(). Instead returns the proposed result dict (with policy check result). Add `commit_refund(customer_id, order_id, amount, policy_approved, services) -> str` that calls services.financial_system.process_refund() and returns JSON. Keep handle_process_refund() as the simple path (calls propose then commit) for backward compat without callbacks.

    2. **handlers.py**: Extend dispatch() signature to `dispatch(tool_name, input_dict, services, context=None, callbacks=None)`. Logic:
       - If tool_name == "process_refund" and callbacks has "process_refund": call propose_refund, run callback(tool_name, input_dict, proposed_result, context, services). If action="block", return callback.replacement. If action="allow", call commit_refund and return result.
       - For other tools: call handler normally. If callbacks has tool_name: run callback(tool_name, input_dict, json.loads(result), context, services). If action="replace_result", return callback.replacement. Otherwise return original result.
       - If no callback for tool_name, return handler result as before.

    3. **agent_loop.py**: Add `callbacks: dict | None = None` parameter to run_agent_loop(). Create context dict: `{"user_message": user_message}`. Pass context and callbacks to every dispatch() call. Update the dispatch call at ~line 112 to: `dispatch(block.name, block.input, services, context=context, callbacks=callbacks)`.

    4. **agent/__init__.py**: Add `build_callbacks, CallbackResult` to imports and __all__.

    Ensure ALL existing tests still pass (backward compatibility). The dispatch() defaults (context=None, callbacks=None) mean all existing callers work unchanged.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/cca_exam/customer_service && poetry run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "propose_refund" src/customer_service/tools/process_refund.py
    - grep -q "commit_refund" src/customer_service/tools/process_refund.py
    - grep -q "callbacks" src/customer_service/tools/handlers.py
    - grep -q "callbacks.get(tool_name)" src/customer_service/tools/handlers.py
    - grep -q "context" src/customer_service/agent/agent_loop.py
    - grep -q "callbacks" src/customer_service/agent/agent_loop.py
    - grep -q "def test_veto_guarantee" tests/test_callbacks.py
    - grep -q "get_processed" tests/test_callbacks.py
    - grep -q "build_callbacks" src/customer_service/agent/__init__.py
  </acceptance_criteria>
  <done>
    dispatch() accepts context and callbacks, routes per-tool. process_refund is two-step vetoable: propose then commit. Blocked refund leaves FinancialSystem untouched (veto guarantee test passes). All existing tests pass unchanged. agent_loop passes context and callbacks through.
  </done>
</task>

</tasks>

<verification>
poetry run pytest tests/ -x -q
poetry run ruff check src/ tests/
</verification>

<success_criteria>
- Per-tool callback dict dispatches only to the registered tool's callback
- process_refund blocked for C003 $600: returns {"status": "blocked", "action_required": "escalate_to_human"}
- FinancialSystem.get_processed() empty after blocked refund (veto guarantee)
- Credit card regex redacts 4111-1111-1111-1111 to ****-****-****-1111
- All existing Phase 2 tests still pass (backward compatibility)
- Ruff lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-callbacks-enforcement-and-first-notebooks/03-01-SUMMARY.md`
</output>
